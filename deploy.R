library(rsconnect)

# official version ----

deployApp(account = 'psrcwa',
         appName = 'planning-academy-schools-2024', # name of your app in the url https://psrcwa.shinyapps.io/dashboard-name
         appTitle = 'Summer Planning Academy Schools Dashboard') # name that will be displayed in the window title